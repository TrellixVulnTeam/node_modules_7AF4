import React from "react";
import { Toggle } from "./index";

export default {
  title: "MetLife APAC UI Library/Toggle",
  component: Toggle,
  argTypes: {},
};

const Template = (args) => <Toggle {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  testId: "",
  checked: false,
  containerClass: "",
  disabledSwitch: false,
  onValueChange: () => {},
  onText: 'ON',
  offText: 'OFF'
};
