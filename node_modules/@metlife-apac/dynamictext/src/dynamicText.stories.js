import React from "react";
import { DynamicText } from "./index";

export default {
  title: "MetLife APAC UI Library/DynamicText",
  component: DynamicText,
  argTypes: {},
};

const Template = (args) => <DynamicText {...args}>{args.label}</DynamicText>;

export const Primary = Template.bind({});
Primary.args = {
  className: "policyList__list-name",
  defaultSize: 20,
  children: "",
  label: "Text",
};
