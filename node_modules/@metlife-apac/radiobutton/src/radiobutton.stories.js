import React from "react";
import { RadioButton } from "./index";

export default {
  title: "MetLife APAC UI Library/RadioButton",
  component: RadioButton,
  argTypes: {},
};

const Template = (args) => <RadioButton {...args} />;

export const Primary = Template.bind({});
Primary.args = {
  testId: "",
  className: "",
  checked: true,
  label: "Radiobutton",
  onValueChange: () => {},
  isCheckIcon: true
};
