import React from "react";
import { Header } from "./index";

export default {
  title: "MetLife ASIA Digital Servicing Library/Header",
  component: Header,
  argTypes: {},
};

const Template = (args) => (
  <div className="policy">
    <Header {...args} />
  </div>
);

export const Primary = Template.bind({});
Primary.args = {
  testID: "header",
  onExpand: () => {},
  onHeaderClick: () => {}
};
