import React from "react";
import { Checkbox } from "./index";

export default {
  title: "MetLife APAC UI Library/Checkbox",
  component: Checkbox,
  argTypes: {},
};

const Template = (args) => (
  <div className="login">
    <form className="login__container" onSubmit={() => {}}>
      <Checkbox {...args} />
    </form>
  </div>
);

export const Primary = Template.bind({});
Primary.args = {
  label: "Checkbox",
  value: true,
  onCheck: () => {},
  className: "",
  onClickIcon: () => {},
  testId: "",
  disabled: false,
  rounded: false
};
